name: flutter_iot_pet_app
description: A minimal Flutter project covering Riverpod, clean arch, mocks.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^0.13.6
  riverpod_annotation: ^2.3.3
  geolocator: ^11.0.0
  web_socket_channel: ^2.4.0
  graphql_flutter: ^5.1.2
  flutter_blue_plus: ^1.35.5
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  build_runner: ^2.4.7
  riverpod_generator: ^2.3.6
  golden_toolkit: ^0.15.0
  mocktail: ^1.0.1

flutter:
  uses-material-design: true